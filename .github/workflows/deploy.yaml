name: EFT - test, build, push y deploy a EKS

on:
  push:
    branches: [ deploy ]
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  CLUSTER_NAME: microservicios-eks

jobs:
  # ===========================================================
  # ETAPA 1 - PRUEBAS UNITARIAS
  # ===========================================================
  test:
    name: Pruebas unitarias (pytest bonos-service)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Instalar dependencias
        run: pip install -r bonos-service/requirements.txt
      - name: Ejecutar pruebas
        run: pytest bonos-service

  # ===========================================================
  # ETAPA 2 - BUILD DE IMÁGENES
  # ===========================================================
  build:
    name: Construir imagen Docker
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Login en Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build imagen bonos-service
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t "$REGISTRY/bonos-service:$IMAGE_TAG" -t "$REGISTRY/bonos-service:latest" ./bonos-service

  # ===========================================================
  # ETAPA 3 - PUSH A ECR
  # ===========================================================
  push:
    name: Push a Amazon ECR
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - name: Login en Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Push imagen bonos-service
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push "$REGISTRY/bonos-service:$IMAGE_TAG"
          docker push "$REGISTRY/bonos-service:latest"

  # ===========================================================
  # ETAPA 4 - DEPLOY EN EKS
  # ===========================================================
  deploy:
    name: Deploy en EKS
    runs-on: ubuntu-latest
    needs: push
    steps:
      - uses: actions/checkout@v4
      - name: Configurar credenciales de AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}
      - name: Instalar kubectl
        run: |
          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl
      - name: Conectar kubectl al cluster
        run: aws eks update-kubeconfig --name "$CLUSTER_NAME" --region "$AWS_REGION"
      - name: Aplicar manifests bonos-service
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          sed -i "s|<ACCOUNT_ID>|$ACCOUNT_ID|g; s|<REGION>|$AWS_REGION|g; s|bonos-service:latest|bonos-service:$IMAGE_TAG|g" bonos-service/k8s/deployment.yaml
          kubectl apply -f bonos-service/k8s/deployment.yaml
          kubectl apply -f bonos-service/k8s/service.yaml
      - name: Verificar rollout
        run: kubectl rollout status deployment/bonos-service --timeout=180s
